---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Release on Merge

concurrency:
  group: container-release
  cancel-in-progress: false

on:
  push:
    branches: ["main"]
    paths:
      - "apps/**"
      - "scripts/templates/**"
      - "!apps/**/metadata.yaml"
      - "!apps/**/README.md"

jobs:
  simple-checks:
    uses: auricom/containers/.github/workflows/simple-checks.yaml@60139f1456d61d1f6fd09c264ee9651bc0330f08 # main

  get-changed-images:
    needs: ["simple-checks"]
    uses: auricom/containers/.github/workflows/get-changed-images.yaml@60139f1456d61d1f6fd09c264ee9651bc0330f08 # main

  build-images:
    needs: ["simple-checks", "get-changed-images"]
    if: ${{ needs.get-changed-images.outputs.addedOrModified == 'true' }}
    uses: auricom/containers/.github/workflows/build-images.yaml@60139f1456d61d1f6fd09c264ee9651bc0330f08 # main
    secrets: inherit
    with:
      appsToBuild: "${{ needs.get-changed-images.outputs.addedOrModifiedImages }}"
      pushImages: true
      sendNotifications: true

  render-readme:
    name: Render Readme
    needs: build-images
    if: ${{ always() && needs.build-images.result != 'failure' }}
    uses: auricom/containers/.github/workflows/render-readme.yaml@60139f1456d61d1f6fd09c264ee9651bc0330f08 # main
    secrets: inherit
